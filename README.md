Simple `hash_map` 🌱 implementation in C++ for numerics-based keys. This program requires an immensely heavy and complex building and compiling toolchain, use with care:

```
g++ -std=c++17 -o main main.cxx
```